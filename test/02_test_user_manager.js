/**Test if add user correctly
 * 1. Duplicate username
 * 2. Information stored in the file correctly match the input
 * 3. Can store multiple users (add username should not overwrite current file)
 * 4. Prevent SQL injection (to be implemented in the future)
 * 5.
 *
 */

/**
 * Test if validification is correct
 * 1. Wrong password should block out user
 * 2. Username not registered should also return same message (prevent attacker to know which usernames are registered)
 */
